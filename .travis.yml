#language: node_js
#node_js: 8

#before_script:
#  - npm install firebase-tools -g
#  - cd functions
#  - npm install
  
#script:
#  - firebase deploy --only functions --project "$PROJECT_ID" --token "$TOKEN"
deploy:
  provider: firebase
  token:
    secure: "$TOKEN"
  project: "$PROJECT_ID"
